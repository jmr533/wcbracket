# wcbracket

## Vercel setup

1. Add Upstash Redis from the Vercel Marketplace to the `wcbracket` project.
2. Add `ADMIN_PASSWORD` to the Production, Preview, and Development environments.
3. Redeploy, then open `/?admin=1` to sign in. The admin entry point is not linked publicly.
